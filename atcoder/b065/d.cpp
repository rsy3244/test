#include <iostream>
using namespace std;
int main(){
    int n;
    cin >> n;
    vector<pair<int,int> > p(n);
    for(int i=0;i<n;i++){
        cin >> p[i].first >> p[i].second;
    }
    vector<bool> used(n,false);
    while(true){
        int v=-1;
        for(int i=0;i<n;i++){
            if(!used[])
        }
    }

    return 0;
}