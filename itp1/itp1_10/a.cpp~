#include <bits/stdc++.h>

using namespace std;

int main(){
	pair<double,double> p1,p2;
	cin >> p1.first >> p1.second;
	cin >> p2.first >> p2.second;
	double dis;
	dis=hypot(p2.first-p1.first,p2.second-p2.first);
	printf("%f\n",dis);
	return 0;
}
